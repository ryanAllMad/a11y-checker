		// some code to get redundant links

		// eslint-disable-next-line no-console
		href.forEach((h) => console.log(h.getAttribute('href')));

		const href = hasElement(`.editor-styles-wrapper a[href]`);

				<PanelRow>
					<h3>
						We found redundant links. Mark to hide them from screen
						readers.
					</h3>
				</PanelRow>
